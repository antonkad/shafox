{
  "name": "shafox",
  "version": "1.0.0",
  "private": true,
  "description": "Every commit wears a different fox. A living demo of kad.dev's per-commit deploy timeline.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --host --port 4173",
    "start": "node server/index.js",
    "serve": "npm run build && node server/index.js"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
